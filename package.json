{
  "builds": [
    {
      "src": "function.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["yt-dlp"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/function.js"
    }
  ]
}
